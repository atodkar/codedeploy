version: 0.1

phases:
  build:
    commands:
      - echo Build completed on `date`
      - mvn compile

  post_build:
    commands:
      - mvn package
      - mkdir artifact
      - unzip target/SampleMavenTomcatApp.war -d artifact/
artifacts:
    files:
        - artifact/**/*
    name: artifact
